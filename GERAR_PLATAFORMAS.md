# Gerar pastas de plataforma

Depois de descompactar o projeto, execute na raiz:

```bash
flutter create .
```

Para gerar apenas plataformas específicas, exemplos:

```bash
flutter create . --platforms=web
flutter create --platforms=windows,macos,linux .
```
